apiVersion: v1
kind: Service
metadata:
  name: {{ include "dash-demo.fullname" . }}
  labels:
    {{- include "dash-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dash-demo.selectorLabels" . | nindent 4 }}
